## 技术栈

GAS系统（Gameplay ability,effect,attribute),ai行为树(decorator, service,task),动画蓝图（linked anim graph,linked anim layer, animnotify state, anim montage）。

## 项目介绍    

这是一个基于GAS系统的数据驱动的arpg战斗框架项目，使用actor component思想，增强代码复用性，支持轻重连击，防御，翻滚，锁定敌人操作，敌人会根据攻击的方向，强度等播放不同动画，除了常规的血量外，我还引入了韧性系统，攻击韧性过高的敌人不会造成硬直，韧性较低的敌人则会有硬直。在动画上，我使用了linked anim layer，和linked anim graph以提高复用率。 对动作蓝图和GAS有较深研究，但是对运镜，视听效果熟悉度不高。





## 机制流程

**组件化**：战斗系统逻辑集中在组件中，方便复用。

**韧性机制**：比大小，攻击韧性大就有受击动画，否则打不动，根据大的幅度也会有不同程度的动画。

**数据驱动**：主要集中在武器和角色招式上，使用DataAsset来存储武器数据，包括武器模型，轻重连招数据资产（后续介绍），武器对应的方向性受击，翻滚动画，还有对应动画蓝图（使用linked anim layer）。   对于每个招式，同样采用data asset，主要存储对应招式的动画蒙太奇，和伤害，韧性值；对于动画扭曲的位移时机，hitbox的生成时机等，都在蒙太奇中通过notifystate实现。

**招式的动画蒙太奇**：蒙太奇里会存储招式的hitbox生成时机，动画扭曲的位移时机，可取消时机，这是为了方便直观的配置。

**连招实现原理**：只有轻重连招两种连招树，当互相切换时前一个连招树的计数会清零，连招流程：有两个gameplay ability，在战斗系统组件中也有两个连击计数器，分别对应轻重攻击，进行轻攻击时，会读取轻的连击计数器，播放对应动画，同时把重的连击计数器清零，反过来也是一样。

**攻击实现**：通过notify state在攻击时调用combat comp的hit detection函数。函数里会通过socket获取生成hitbox的位置，然后坐一次碰撞检测，检测打到东西是否有ability system comp，有则发送。

**避免同帧多次击中**:维护hitactor array，攻击结束清除一次。

防御：防御会提升韧性，按下防御键时通过gameplay effect来增加韧性值，在解除防御后会移除gameplay effect。

gameplay tag**互斥管理**：

攻击：在翻滚，受击动画时无法攻击，采用activation blocked tag，当有翻滚或者受击对应的tag时不会触发。

**翻滚实现原理**：

根据输入的vector2d计算出翻滚方向，然后通过FGameplayAbilityTargetData 传入。

**受击方向计算**：

**AI系统**：

基于behavior tree，使用了decorator和service结点。

## 可拓展

更多情况的攻击动画 跳跃时，

防御方向化

hitbox只有一种



## 可能问题：

## 一、GAS系统（核心必问）

1. **为什么选择GAS而不是自己写一套技能系统？GAS解决了什么问题？如果不用GAS你会怎么实现？GAS有哪些缺点？**
2. **`GameplayAbility`、`GameplayEffect`、`GameplayAttribute`分别负责什么？它们的执行流程是怎样的？**
3. **Attribute变化是如何同步的？`Instant` / `Duration` / `Infinite` 三种Effect有什么区别？**
4. **你的攻击技能是Ability还是普通函数？为什么？Ability如何开始、结束、取消？如何控制输入连击？**
5. **`GameplayTag`在你的项目中主要解决了什么问题？为什么不用bool变量？Tag的层级结构是如何设计的？**
6. **你这个项目支持网络同步吗？如果支持，`Prediction`是怎么做的？Ability在Client还是Server执行？如果不支持，GAS天然支持网络同步的特点你了解吗？**

## 二、数据驱动设计

1. **为什么用`DataAsset`而不是`DataTable`来存储武器和招式数据？两者各自的优缺点是什么？**
2. **如果策划要新增一把武器，需要改代码吗？请描述整个配置流程。**
3. **连招为什么也做成`DataAsset`？连招顺序如何控制？动画和伤害如何绑定？**
4. **数据驱动在你项目中的核心价值是什么？举一个如果没有数据驱动会很难维护的例子。**

## 三、战斗系统机制

1. **韧性系统是如何计算的？韧性是否会恢复？Boss和普通敌人的韧性设计有何不同？为什么不用硬直概率？**
2. **攻击判定（Hitbox）是怎么实现的？为什么把Hitbox生成写在`AnimNotifyState`里？如何避免一帧多次命中同一目标？**
3. **防御如何提升韧性？用`GameplayEffect`实现有什么好处？解除防御时如何保证Effect正确移除？**
4. **翻滚的实现原理：如何根据输入向量计算方向？`FGameplayAbilityTargetData`是如何传递数据的？**
5. **受击方向如何计算？不同方向的受击动画是如何切换的？**

## 四、动画系统

1. **为什么使用`Linked Anim Layer`和`Linked Anim Graph`？它们提高了哪些部分的复用性？如果不使用会有什么问题？**
2. **`AnimMontage`和`AnimStateMachine`分别在什么场景下使用？为什么？**
3. **Root Motion 和 Motion Warping（动画扭曲位移）有什么区别？你在项目中是如何使用Motion Warping的？**
4. **动画蓝图中如何处理受击方向？有没有遇到动画与GAS状态不同步的问题？如何解决？**

## 五、连招系统

1. **轻重连击为什么要用两个计数器？为什么不用一棵完整的连招树？**
2. **连招切换时清零另一个计数器的逻辑是怎样的？网络环境下如何保证计数器同步？**
3. **如果要做更复杂的连招（如方向输入、武器不同、技能取消），你会如何扩展当前框架？**

## 六、AI系统

1. **`Decorator`和`Service`在Behavior Tree中的职责有什么区别？你在项目中分别用了哪些？**
2. **为什么用行为树而不用状态机来管理AI？行为树的优势是什么？**
3. **AI攻击是如何触发的？如何判断攻击距离？如何避免AI一直攻击？**
4. **AI是否会响应玩家的攻击方向？例如根据玩家位置选择不同的受击动画？**

## 七、架构设计与拓展性

1. **为什么把战斗系统设计为`ActorComponent`？这种设计带来了哪些好处？**
2. **你的项目中哪些部分体现了“高复用性”？请举例说明。**
3. **如果现在要增加一个新武器类型（如弓箭），你需要改动哪些地方？**
4. **如果要做多人游戏，你的架构需要做哪些修改？**
5. **你文档中提到“更多情况的攻击动画（如跳跃时）”和“防御方向化”是可拓展点，你会如何设计实现它们？**

## 八、开放性问题（高频）

1. **在这个项目里，你觉得做得最好的设计是什么？为什么？**
2. **如果重做一次，你会改进哪里？为什么？**